name: fullstack-demo 
on:
    push:
        branches:
            - main
jobs:
    integration:
        runs-on: ubuntu-latest
        steps:
           - name: code checkout
             uses: actions/checkout@v4
           - name: App build
             run: python3 app.py